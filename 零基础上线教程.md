# 零基础上线教程

> **2026 更新**：若需 **账号登录、历史记录、充值余额**，请阅读 **[DEPLOY-FINAL.md](./DEPLOY-FINAL.md)**，使用 VPS 或 Docker。  
> 下文 Netlify 方式适合**无登录**的轻量演示。

# 零基础上线教程（Netlify 轻量版）（GitHub + Netlify）

照着做即可，**不用** GitHub 首页那个「Ask anything」AI 框，也**不要**拖拽文件夹。

---

## 第一步：在 GitHub 创建仓库（2 分钟）

1. 打开 https://github.com 并登录（你已是 wangjiaxin263669@gmail.com）
2. 看页面**左上角**或**左侧**，点绿色按钮 **「Create repository」**（创建仓库）
   - 若找不到：点右上角 **+** → **New repository**
3. 填写：
   - **Repository name**：`cross-culture-map`（可自定，记住名字）
   - 选 **Public**（公开，Netlify 免费版需要）
   - **不要**勾选 "Add a README"（我们本地已有代码）
4. 点最下面绿色 **「Create repository」**
5. 创建后会看到一个地址，类似：
   `https://github.com/你的用户名/cross-culture-map.git`  
   **先不要关这个页面**，下一步要用

---

## 第二步：把电脑上的代码推到 GitHub（3 分钟）

在 **Cursor 底部终端**（或 PowerShell）执行下面命令。  
把 `你的用户名` 换成你 GitHub 主页地址里的名字。

```powershell
cd c:\Users\ASUS\Desktop\cross-culture-map\cross-culture-map

git branch -M main

git remote add origin https://github.com/你的用户名/cross-culture-map.git

git push -u origin main
```

- 第一次 push 会弹出 **登录 GitHub**（浏览器或窗口），按提示登录即可
- 成功后刷新 GitHub 仓库页面，应能看到很多文件

> 本地已帮你执行过 `git init` 和第一次提交，你只需 `remote add` + `push`。

若提示 `remote origin already exists`，先执行：
`git remote set-url origin https://github.com/你的用户名/cross-culture-map.git`

---

## 第三步：Netlify 连接 GitHub（5 分钟）

1. 打开 https://app.netlify.com 并登录（可用 GitHub 账号直接登录）
2. 点 **「Add new project」** → **「Import an existing project」**
3. 选 **GitHub** → 授权 → 在列表里点 **cross-culture-map**
4. 页面显示构建设置时 **不用改**（自动读取 `netlify.toml`）
5. 点 **「Add environment variables」** 展开，添加：

   | Key | Value |
   |-----|--------|
   | `DEEPSEEK_API_KEY` | 你的 DeepSeek 密钥（sk- 开头） |
   | `NODE_VERSION` | `20` |

6. 点 **「Deploy cross-culture-map」**，等待约 3 分钟
7. 出现绿色 **Published** 后，点站点域名，例如：  
   `https://xxxxx.netlify.app`  
   这就是你的**永久在线地址**

---

## 第四步：检查是否成功

1. 打开你的 `https://xxxxx.netlify.app`
2. 能看到地球和「跨文化研究设计」文字
3. 浏览器访问：`https://xxxxx.netlify.app/api/health`  
   应看到 `"aiConfigured":true`
4. 试一下 AI 聊天

---

## 以后改代码怎么更新？

```powershell
cd c:\Users\ASUS\Desktop\cross-culture-map\cross-culture-map
git add .
git commit -m "更新说明"
git push
```

Netlify 会**自动**重新部署，无需再点按钮。

---

## 常见问题

**Q：GitHub 要付费吗？**  
A：公开仓库免费。

**Q：Netlify 要付费吗？**  
A：个人项目免费额度够用。

**Q：密钥会泄露吗？**  
A：`.env` 不会上传；只在 Netlify 网站设置里填 `DEEPSEEK_API_KEY`。

**Q：报告生成失败？**  
A：Netlify 免费版 AI 有 10 秒限制，聊天一般正常；长报告可升级 Pro 或见 `DEPLOY-CN.md` 用国内服务器。

---

## 需要帮助时发给同学/老师的链接

部署成功后分享：`https://你的站点.netlify.app`
